# Cross-agent adapter

This package uses plain Agent Skills frontmatter plus ICM files. Any harness that can read a folder can use it.

Required capabilities by stage are discovered, not assumed. If the harness lacks subagents, simulate builder/critic separation with separate fresh sessions or sequential fresh contexts. If it lacks browser/runtime tools, mark those gates blocked rather than inventing proof.

Never translate missing tooling into a false success state.
