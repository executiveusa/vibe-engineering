# {{PROJECT_NAME}}

**Status:** STRUCTURE READY — product not yet built or production-verified.

This is an owner-controlled Vibe Engineering workspace for {{PROJECT_AUDIENCE}} in {{PROJECT_DOMAIN}}.

## Start here

1. Read `AGENTS.md` for the laws and boundaries.
2. Read `CONTEXT.md` for the workspace map.
3. Open `stages/00_intake/CONTEXT.md`.
4. Put approved source material in `references/`.
5. Keep current-run work in the relevant stage `output/` folder.

## Verify the workspace

```bash
npm run factory:doctor
```

A PASS proves the folder contracts exist. It does not prove the application works, is safe, is accessible, is deployed, or has customers.

## Project mode

`{{PROJECT_MODE}}`

Brownfield work uses this as a separate control workspace. It must inspect the existing product before proposing changes and must not overwrite the source repository.
