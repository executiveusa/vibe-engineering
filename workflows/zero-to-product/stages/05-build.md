# 05 Build

Use `build`; pull in `test-first`, `debug`, and `deep-work` when the slice needs them.

**Question:** Can we make one complete slice without losing the approved intent?

**Output:** working code or product behavior through an observable seam, with relevant checks passing.
