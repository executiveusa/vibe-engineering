# 04 Plan

Use `tickets`, with `architecture-check`, `module-design`, and `triage` only where they reduce risk or complexity.

**Question:** What is the smallest safe sequence from here to the outcome?

**Output:** ordered end-to-end slices with blockers, owners, dependencies, checks, and integration points.
