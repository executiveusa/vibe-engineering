# 03 Outcome

Use `map`, `spec`, and `prototype` when one uncertainty needs cheap validation.

**Question:** What does good look like before we build?

**Output:** user outcome, standard, boundaries, acceptance checks, proof plan, rollback, and non-goals.
